len_base = float(input("Input the length of base: "))
len_hypo = float(input("Input the lenght of hypotaneous: "))
are_of_triangle = (len_base * len_hypo) * .5
print("The area of triangle is:", are_of_triangle)
